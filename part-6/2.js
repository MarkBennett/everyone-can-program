// Make a new object called numbers who's keys are the numbers 1 to 5, and
// who's values are the numbers as strings. For example numbers[1] should
// equal "One".








// DON'T EDIT THE CODE BELOW

$(function() {
	say("The first five numbers are...");
	for (var i = 0; i < 5; i++) {
		say("" + names[i+1] + "!");
	}	
});
