// There's a message hidden in this array
var arr = ["super ", "powered ","Ninja!", "JavaScript ", "a ", "You're "];

// Your job is to re-assemble it into this string
var msg = "";

// pop() off the last two words and add them to the msg
// msg = msg + ???;

// shift() the first two words from the arr and add them to the msg

// pop() the last two words off the arr and add them to the msg

say(msg);